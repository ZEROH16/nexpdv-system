{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build -w @nexpdv/shared && npm run prisma:generate:prod -w @nexpdv/api && npm run build -w @nexpdv/api"
  },
  "deploy": {
    "startCommand": "npm run start -w @nexpdv/api",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
